./change_ip_and_mac.sh 22


#include <udpclient.h>

char *broadcast_address = "10.255.255.255";
int broadcast_port = 7891;

unsigned *buffer;
int buffer_size = 2;

// Sets up UDP broadcast client.
if (udp_client_setup(broadcast_address, broadcast_port) == 1) {
	return 1;
}

while (udp_client_recv(buffer,buffer_size) != 1) {
	// copy buffer content into audio IP input register.
}


